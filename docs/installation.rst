.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install autolysis

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv autolysis
    $ pip install autolysis
